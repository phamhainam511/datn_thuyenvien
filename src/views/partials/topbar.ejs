<nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">
    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
        <i class="fa fa-bars"></i>
    </button>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown no-arrow mx-1">
            <a class="nav-link dropdown-toggle" href="#" id="alertsDropdown" role="button"
                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-bell fa-fw"></i>
                <span class="badge badge-danger badge-counter">
                    <%= typeof notifications !== 'undefined' && notifications.totalCount > 0 ? notifications.totalCount + '+' : '0' %>
                </span>
            </a>
            <div class="dropdown-list dropdown-menu dropdown-menu-right shadow animated--grow-in"
                aria-labelledby="alertsDropdown">
                <h6 class="dropdown-header">
                    Thông báo
                </h6>
                <% if (typeof notifications !== 'undefined' && notifications.expiringCertificatesCount > 0) { %>
                <a class="dropdown-item d-flex align-items-center" href="/cer-expiring">
                    <div class="mr-3">
                        <div class="icon-circle bg-warning">
                            <i class="fas fa-exclamation-triangle text-white"></i>
                        </div>
                    </div>
                    <div>
                        <div class="small text-gray-500"><%= new Date().toLocaleDateString('vi-VN') %></div>
                        <span class="font-weight-bold">Có <%= notifications.expiringCertificatesCount %> chứng chỉ sắp hết hạn</span>
                    </div>
                </a>
                <% } %>
                <% if (typeof notifications !== 'undefined' && notifications.recentBoardingsCount > 0) { %>
                <a class="dropdown-item d-flex align-items-center" href="/danh-sach-thuyen-vien">
                    <div class="mr-3">
                        <div class="icon-circle bg-primary">
                            <i class="fas fa-ship text-white"></i>
                        </div>
                    </div>
                    <div>
                        <div class="small text-gray-500"><%= new Date().toLocaleDateString('vi-VN') %></div>
                        <span class="font-weight-bold"><%= notifications.recentBoardingsCount %> thuyền viên chuẩn bị lên tàu</span>
                    </div>
                </a>
                <% } %>
                <% if (typeof notifications === 'undefined' || notifications.totalCount === 0) { %>
                <a class="dropdown-item d-flex align-items-center" href="#">
                    <div>
                        <span class="font-weight-bold">Không có thông báo mới</span>
                    </div>
                </a>
                <% } %>
            </div>
        </li>

        <div class="topbar-divider d-none d-sm-block"></div>
        <li class="nav-item dropdown no-arrow">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <span class="mr-2 d-none d-lg-inline text-gray-600 small">
                    <%= typeof user !== 'undefined' && user ? user.hoten : 'Khách' %>
                </span>
                <img class="img-profile rounded-circle" src="/img/undraw_profile.svg">
            </a>
            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
                aria-labelledby="userDropdown">
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.phanquyen && user.phanquyen.tenphanquyen) { %>
                        <a class="dropdown-item" href="#">
                            <i class="fas fa-user-lock mr-2 text-gray-400"></i>
                            <%= user.phanquyen.tenphanquyen %>
                        </a>
                    <% } %>
                    <a class="dropdown-item" href="#">
                        <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                        Tài khoản: <%= user.taikhoan %>
                    </a>
                <% } %>
                <a class="dropdown-item" href="/doi-mat-khau">
                    <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
                    Đổi mật khẩu
                </a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">
                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                    Đăng xuất
                </a>
            </div>
        </li>
    </ul>
</nav>
